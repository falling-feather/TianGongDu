# 16｜测试、CI 与发布门禁

> 上级文档：[`../01-开发者文档.md`](../01-开发者文档.md)
> 状态：Active Baseline
> 最后更新：2026-07-11
> 维护者角色：QA/发布负责人；各模块 Owner 对对应测试和证据负责

## 1. 目标

自动化必须同时守住 C++ 正确性、WASM/浏览器生命周期、内容契约、本地存档、云同步和发布缓存。原生测试快不等于网页可玩；浏览器能跑一遍也不等于跨设备不会丢档。

## 2. 测试层级

| 层 | 运行频率 | 覆盖 |
| --- | --- | --- |
| 静态 | 每次提交 | clang-format/tidy、编译、include 层级、Schema、ID、文档链接 |
| Native 单元 | 每次提交 | C++ Runtime/Gameplay/ContentCore/迁移/合并 |
| Native 属性/模糊 | PR/夜间 | 解析器、Operation、任务图、效果、存档 |
| WASM 单元/集成 | 每次 PR | 编译差异、桥、内存、异常策略、内容加载 |
| 浏览器场景 | PR/夜间 | Canvas、DOM、输入、音频、IndexedDB、生命周期 |
| 核心回放 | 每次 PR 核心集 | Web Single + Native Core 离散状态一致；不等待完整 Windows 宿主 |
| Pthreads 回放 | Threads 通道建立后的受影响 PR/夜间 | Single/Pthreads 结果一致与性能收益；Deferred 时不阻塞 Single PR |
| 同步合同/服务 | IDL 建立前每次 PR 跑 mock 合同；服务建立后的受影响 PR | API、PostgreSQL 事务、幂等、冲突、迁移 |
| 端到端多设备 | Sync Spike 建立后夜间/候选版 | 两浏览器/Native 分叉、合并、账号链接 |
| 性能/网络 | 夜间/里程碑 | 帧、WASM/JS/GPU 内存、下载、缓存、弱网 |
| 人工游玩 | 每周/里程碑 | 节奏、读招、内容深度、浏览器 UX、辅助 |
| 部署/回滚 | 候选版 | CDN/Service Worker/API/DB 灰度与回滚 |

## 3. C++ 静态与单元门

- Clang/MSVC 至少两套编译器在支持矩阵构建纯核心。
- 警告视为错误；例外按规则编号和范围抑制。
- clang-tidy/自定义检查禁止跨层 include、裸 owning pointer、平台宏进 Gameplay。
- ASan/UBSan 原生夜间运行；适用时 MSan/TSan 独立实验。
- 单元测试注入时钟、随机、存储、网络和物理；不访问真实浏览器/数据库。
- 属性测试覆盖效果叠加、任务幂等、Operation 重放、合并交换/结合条件和迁移。
- 模糊测试内容包、快照、操作和 API payload 解析器。

## 4. WASM 构建矩阵

| 产物 | 必选 | 用途 |
| --- | --- | --- |
| `web-debug` | 是 | source map、断言、桥诊断、浏览器测试 |
| `web-release-single` | 是 | 1.0 兼容/发布基线 |
| `web-release-pthreads` | 条件 | COOP/COEP 环境性能增强 |
| `native-windows-debug` | 是 | 快速调试和回放对照 |
| `native-windows-release` | F1N 后 | 原生 Beta/性能对照 |

Web 构建测试导出符号白名单、WASM 体积、静态初始化、异常/RTTI/LTO 参数、初始/最大内存和资源清单。非预期导出、体积或依赖增长超过阈值即失败。

## 5. 浏览器自动化

真实浏览器驱动完成：

- 冷缓存/热缓存启动、能力探测和加载进度。
- 键鼠、手柄模拟、DOM 焦点、全屏、DPR/缩放和窗口尺寸。
- `visibilitychange`, `pagehide`, blur/focus, reload, WebGL context lost/restored。
- IndexedDB 保存、事务失败、配额、旧 Schema、多标签写锁。
- Service Worker 首装、等待、激活、旧标签、新版本、离线和回滚。
- Web 单线程必跑；线程版只在正确头部环境跑并检查选择器。
- 控制台 error、unhandled rejection、CSP/COEP 违规和长任务为测试产物。

Chrome/Edge/Firefox 为主要 CI 矩阵；Safari 需要 macOS runner/人工门。不能用一个 Chromium 结果代表所有浏览器。

## 6. DOM/可访问性

- 语义/ARIA/焦点图、隐藏控件、按键提示、文本溢出和本地化。
- Canvas 关键状态有 DOM 等价摘要。
- 键盘、手柄、触控布局切换不产生双输入。
- 深层菜单暂停 GameCore，关闭恢复正确 Action Context。
- 200% 浏览器/UI 缩放、无色、静音、减少动态和屏幕阅读菜单路径。
- DOM 更新频率/节点数和 layout/paint 纳入性能。

## 7. 存档测试

- IndexedDB 事务中断、刷新、浏览器崩溃、配额、存储清理提示和导出。
- 最近 N 个 SaveVersion 黄金档在 Web/Windows 读写/迁移。
- Boss 阶段、任务节点、地区归位、包缺失和内容升级往返。
- Native 导出→Web 导入、Web→Native 导入语义一致。
- 1000 次事务和 100 次页面重载后无 head/快照孤儿增长。
- 浏览器多标签只有一个写入者；接管流程不丢 Operation。

## 8. 同步服务测试

### 8.1 单元/集成

- request/operation 幂等、base revision、事务回滚和错误码。
- OIDC subject 映射、Profile 授权、设备撤销和 token 过期。
- PostgreSQL 并发更新、死锁重试、唯一约束和迁移。
- Snapshot/Operation 版本、哈希、压缩/大小限制。
- 领域合并：集合、进度、库存 claim、任务偏序、关系和叙事冲突。

### 8.2 多设备场景

模拟 A/B 从 revision R 离线：

- 不同解锁并集。
- 同一唯一奖励重复领取只生效一次。
- 一端消费、一端获得库存按账本正确。
- 两端推进可兼容任务得到共同后继。
- 互斥叙事选择返回冲突，双方快照保留。
- 冲突选择/复制 Profile 后产生新 revision。
- 网络超时后请求重放不重复应用。

## 9. 回放一致性

同一初始快照、内容包、随机种子和命令流在 Native、Web 单线程、Web Pthreads 运行。比较离散权威状态、事件/Operation 序列和容差规范，不比较 Sprite 像素。

出现差异时报告首个 divergent Tick、组件、浮点量化、容器顺序和平台信息。不得用扩大容差掩盖任务/奖励差异。

## 10. 内容与编辑器

- Native CLI 与 ContentCore WASM 对黄金输入输出同一规范模型/错误码。
- 模板创建、迁移、引用图、图可达、预算和本地化。
- 编辑器刷新/崩溃恢复、Undo、外部修改、三方合并和大项目性能。
- 预览与正式 Runtime 读取同一 `.tgdpack`。
- 当前 `npm run validate:design` 持续守住文档、内容规模、模板和技术基线。

## 11. 性能与网络

固定硬件、浏览器、DPR 和网络整形。报告 P50/P95/P99、长帧、WASM/JS/GPU 内存、GC、下载、解压、纹理上传、IndexedDB、DOM layout 和同步延迟。

每地区路线：冷缓存、热缓存、弱网、离线、最密战斗、最快穿越、30–60 分钟内存回环。趋势回退超过阈值即失败，即使未越绝对硬门。

## 12. CI 流水线

### F1-DEV-01～02 已实现门

`.github/workflows/f1-clean-build.yml` 已在 Windows 2022 托管 runner 上实现干净构建：固定 action commit 与 Node 20.18.0，恢复后重新校验全部工具缓存和 LLVM 官方签名，执行 MSVC/Clang Debug/Release（每配置含 Native 命令回放）、Axmol Web Single Debug/Release，再以 Chrome/Edge/Firefox 验证八个生命周期/WebGL2 context restore 检查点和 30/60/144 Hz、10,000 Tick WASM 黄金回放。无 GPU CI 显式使用软件 WebGL，并把 graphics mode/renderer、量化姿态、校验值与宿主/探针 BuildIdentity 写入报告；该结果只用于功能可复现性，不替代真实硬件性能门。

每次运行以 `g1-gh-<run-id>-<attempt>` 绑定 commit，并上传浏览器报告、前后帧、Native/Web 产物及 `build/evidence/<build-id>/manifest.json`。构建/宿主首个全绿证据是 `g1-gh-29152364317-1`，Session/输入/回放证据是 `g1-gh-29155512855-1`；以下 PR/夜间/候选版清单仍是完整目标，不能因这些子门落地而视作 G1 全部实现。

### PR 快速门

1. 文档/Schema/技术基线/内容校验。
2. C++ 格式、依赖层级、双编译器核心构建。
3. Native 单元/属性/契约。
4. Web Debug/Release Single 构建、体积检查。
5. Chromium/Firefox 核心浏览器场景。
6. Sync IDL/mock 合同；Sync Service 建立后，受影响 PR 再要求服务单元 + 临时 PostgreSQL 集成。
7. 受影响包确定性烘焙。
8. 依赖、许可证、秘密和 Web 安全头检查。

### 夜间门

全量内容、Pthreads、Safari（可用 runner）、扩展内容回放矩阵、模糊/消毒器、NPC 长模拟、Boss 遍历、长时内存、Service Worker 更新、多设备同步、数据库迁移和性能路线。

### 候选版门

签名清单、CDN staging、旧 Service Worker、旧存档/浏览器缓存、灰度/回滚、OIDC/数据导出删除、三地区全流程、辅助/浏览器矩阵和灾备恢复。

## 13. Flaky 政策

一次受控重跑只收集诊断，原失败仍记录。Flaky 标记负责人、首次日期、原因、到期日；存档、同步、发布和核心浏览器测试不得长期隔离。

测试禁止依赖真实墙钟、公共互联网、执行顺序、作者目录或未固定第三方账号。OIDC/CDN 使用契约模拟；少量 staging 真实集成在候选门执行。

## 14. 缺陷严重度

| 级别 | 示例 | 发布行为 |
| --- | --- | --- |
| Blocker | 无法启动/通关、存档丢失、同步覆盖、安全严重问题 | 禁止合并/发布 |
| Critical | 稳定崩溃、Boss/任务阻断、主要浏览器不可用 | 候选版前为 0 |
| Major | 明显玩法、性能、缓存、辅助或冲突 UX 错误 | 逐项批准，不能批量忽略 |
| Minor | 不阻断局部瑕疵 | 记录负责人和版本 |

## 15. 人工游玩

每地区做灰盒理解、内容节奏、近最终盲测。额外覆盖：首次网页用户是否理解加载/保存/云状态；刷新/离线是否安心；冲突 UI 是否能在不懂“revision”的情况下做决定；键鼠/手柄切换是否顺畅。

测试主持人不现场解释。每地区 5 小时按首玩中位数；下载等待不计入。

## 16. 发布清单

- Blocker/Critical 为 0。
- Web 单线程完整；线程版仅在支持环境启用。
- 内容/可执行/Web Shell/存档/同步兼容矩阵确认。
- 三地区主线/支线/后日常可完成。
- 浏览器、弱网、离线、缓存、内存、辅助和本地化门通过。
- Guest、账号链接、多端分叉/合并、导出/删除通过。
- CDN/Service Worker/API/数据库灰度与回滚通过。
- 签名、SBOM、许可、隐私和第三方通知齐全。
- 产物从受控 commit/工具镜像可重建，哈希留档。

## 17. 验证工作的 Definition of Ready

测试项进入实现前必须具备：目标合同/行为、可创建初始状态、稳定断言、预期错误、环境/版本、Owner、运行频率和失败诊断。性能测试还要固定硬件、浏览器、DPR、网络整形、预热/采样区间和基线 build；人工盲测要有招募条件、任务脚本、主持人禁说事项和计时口径。

没有可测试合同的任务退回设计；没有自动夹具但理由成立的人工项必须写清何时补自动化。测试不得从实现细节猜预期后再自证正确。

## 18. 标准证据包

G1/G2/G3/M1/A0/B0/RC 每个门生成 `evidence/<build-id>/manifest.json` 或 CI 中等价不可变索引，至少链接：

- commit/tag、build/channel、工具链/依赖/SBOM、产物与内容包哈希。
- required checks、单元/集成/浏览器/同步/迁移结果与覆盖范围。
- 浏览器/设备/DPR/输入/网络矩阵和失败重跑记录。
- 原始 trace、网络瀑布、WASM/JS/GPU 内存曲线、长任务和包体报告。
- 标题页起步的纵切录屏；失败/刷新/离线/context restore/冲突解决专项录屏。
- 黄金回放、存档、迁移、内容包和同步 fixture 的 ID/哈希。
- 已知缺陷、Flaky、风险例外、Owner、到期日和玩家影响。
- 02 所定里程碑最终 A 的 Go/No-Go，以及技术、QA、安全/存档红线的会签或否决记录；会签不构成第二个 A。

PR 快速产物可保留 30–90 天；夜间趋势至少覆盖当前里程碑；Stable 发布的清单、黄金档、迁移、性能基线、安全/许可和回滚证据按大版本支持期长期保留。03 记录验证摘要与证据 ID。

当前首个不可变索引为 `g1-gh-29152364317-1`，绑定 commit `19bdcbceb9db9f04eb9025ab9c7939aa982e6fc5`；CI artifact `f1-g1-evidence-29152364317-1` 的 SHA-256 为 `60564b28caf01119c852e98aa2fb83422f82f1a09872fd8b26918fbfaab5ad28`。该 artifact 的在线保留期是 14 天，03 的摘要和哈希不随临时下载链接失效。

## 19. F1 参考环境与可复现性能

G1 锁定前先用以下分层，不把开发者高端机成绩当最低配置：

| 档 | 用途 | 起始参考 |
| --- | --- | --- |
| Baseline Desktop | F1 必选门 | Windows 10/11、4 核主流 CPU、8 GiB RAM、WebGL2 集显、1080p、Chrome/Edge/Firefox Stable |
| Recommended Desktop | 60 FPS 质量档 | 6 核 CPU、16 GiB、主流独显/较新集显、1080p/1440p |
| Stress/Low | 发现降级与包体问题 | CPU 4× slowdown、低档 GPU runner/集显、4 GiB 可用内存压力、30 FPS 渲染档 |
| Safari Spike | 能力边界 | 当前受支持 macOS + Safari；G1/G2 记录但未过门前不宣称正式支持 |

网络基线至少有：20 Mbps/常规延迟冷缓存、5 Mbps/100 ms 弱网、断网、丢包/中断恢复和热缓存。采样排除调试构建和浏览器 DevTools 本身的显著干扰，保留原始配置。趋势失败起始规则：同场景 P95 帧/内存/传输相对最近已接受基线上升 >10% 且超过噪声带则阻断，需 Owner 解释/批准新基线；绝对硬门仍优先。

F1-DEV-01 自动化 runner 已记录为 GitHub Windows 2022；功能矩阵为 Chrome `150.0.7871.47`、Edge `150.0.4078.48`、Firefox `151.0`，并显式强制软件 WebGL。真实 CPU/GPU、DPR、网络与性能基线仍未锁定，后续必须在代表性设备上另取证，不能把托管 runner 或软件渲染成绩当最低配置。

## 20. 机器 Schema 验证成熟度

当前 `npm run validate:design` 会解析 Schema 文件，并以项目语义断言检查数量、ID、引用、文档层级和技术基线；它尚未执行完整 JSON Schema Draft 2020-12 关键字集合。这一限制不得在交接时表述成“所有 Schema 已被标准验证器证明”。

G1/G3 工具链必须引入锁定的 Draft 2020-12 validator，提供：

- 全部现有 Schema 的正向/负向 fixture；错误字段、缺字段、重复平台/variant、非法 ID、错误版本必须失败。
- 内容工作台、Native CLI 与 CI 的同一诊断 code/JSON Pointer。
- pack/save/operation/sync IDL 的机器合同和生成类型。
- Schema 自身 meta-schema 检查、版本迁移和兼容 fixture。

在引入完整 validator 前，手写语义守卫仍保留；二者解决的问题不同，不能用“Schema 通过”替代跨引用、图可达、预算和领域不变量。

## 21. 门禁例外

Blocker/Critical、安全、丢档、重复权益、不可回滚发布和必选浏览器启动失败没有常规例外。Major/性能趋势的临时例外必须包含：具体测试、影响用户、范围、Owner、批准者、到期日、修复里程碑、监控和回滚；到期自动恢复阻断。

测试重跑不能把红变绿后删除第一次失败。调整阈值、黄金档或快照必须在差异评审中说明玩家语义未变；禁止用批量更新黄金文件掩盖回归。
