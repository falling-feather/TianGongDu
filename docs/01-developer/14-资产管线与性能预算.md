# 14｜资产管线与性能预算

> 上级文档：[`../01-开发者文档.md`](../01-开发者文档.md)
> 状态：Web-first Baseline；数值由 F1 真实设备校准
> 最后更新：2026-07-12
> 维护者角色：技术美术负责人；美术指导、动画、Web/表现、音频、许可与 QA 会签

## 1. 美术方向

1.0 采用 2.5D 斜向全景构图：受控俯斜镜头、可纵深走位的地面战斗面、明确脚点与遮挡、前景框景、中景战斗和远景地标共同组织一个全景 Cell。《斗战神》是第一指导，用于角色/场景比例、东方材质重量、空间层次、群战构图与镜头调度；《暖雪》只作为俯斜视角战斗轮廓和输入反馈的第二参考。所有资产必须形成《天工渡》自己的角色、器物、建筑、UI 与动作语言，不得临摹或重制参考作品的可识别表达。

最终表现载体不预先锁死为全 Sprite 或重型全 3D。F1 在同一“雨夜试伞”构图比较 Sprite 帧/骨骼、预渲染层、低复杂度实时 3D 场景或角色及其混合方案，以战斗可读性、角色屏占比、遮挡、下载/显存、动画产能和多端一致性放行。不能让某个专有动画/DCC 工具成为全部角色的不可替换前提；G1 为隔离宿主生命周期而关闭未使用的引擎 3D 模块，不等于正式资产方案已经选择纯 2D。

## 2. 管线原则

- 源资产、导入配置、运行时产物、内容包和 CDN 文件分层。
- 所有发布资源可由锁定工具从源重建。
- 先定网页屏幕贡献、下载/内存预算，再定分辨率和帧数。
- 同类资产共享图集、骨骼、动画状态、材质和碰撞规范。
- 资源包按玩家使用路径分，而不是按美术部门目录分。
- 冷启动、弱网、缓存清理和低内存从灰盒开始测试。

## 3. 目录

```text
assets_src/<domain>/<region>/<asset_id>/
  source.aseprite / source.psd / source.blend / audio project
  references/
  license.yaml
  import.yaml

assets_runtime/<package>/<asset_id>/
  atlases/
  animations/
  audio/
  fx/
  manifest.fragment.json
  previews/
```

资产 ID 为小写英文稳定键。显示名、作者和许可进入 metadata。导入器不能依赖绝对路径、作者机器字体或未锁插件。

## 4. 角色动画

### 4.1 Sprite/预渲染图集基线

- 统一逻辑朝向、脚底锚点、受击盒原点和像素/世界单位。
- 动画定义首帧、关键姿态、接触、恢复和循环语义；Gameplay 窗口仍由 Tick 定义。
- 图集按角色家族/生命周期分页，不能把全地区角色塞进一张图。
- 透明边界裁剪但保留一致锚点；预览自动检查抖动、脚滑和裁切。
- 常见角色共享动作语法，核心 NPC 通过职业动作、轮廓、配色和局部独特动作区分。

### 4.2 骨骼动画

Axmol 提供 Spine 扩展，但实际使用前必须确认编辑器/Runtime 许可、网页包体、版本锁和移动端性能。内容合同只保存通用 Skeleton/Animation ID，不让 Gameplay 依赖 Spine 类型。

骨骼、附件、蒙皮和约束有平台预算；远处/群众降低更新率或切 Sprite 烘焙。Root Motion 只作视觉参考，规则位移在 Runtime。

## 5. 场景与图集

斜向全景子地区按 Cell 组织：远景地标、环境中景、地面可玩平面与高度/层、前景框景与遮挡体、交互/碰撞、特效与音频区。可重复构件实例化，独特地标保留轮廓；遮挡体必须带可淡化/裁切策略和 Gameplay 无关的景深排序 metadata，不能用美术层级替代权威碰撞。

图集默认 2048²；4096² 只有目标浏览器/设备验证、包体和纹理内存允许时使用。UI、角色、环境、特效按过滤模式、色彩空间和生命周期分组。

传输格式与 GPU 压缩在 F1 Render Spike 锁定。不能假设所有浏览器支持同一压缩扩展；清单按能力选择资源变体，始终有受预算的兼容版本。

## 6. 音频

总线：Master、Voice、Music、Ambient、SFX、Combat、UI、Accessibility。首次用户手势后再恢复 Web Audio，处理标签页静音/挂起。

- 短 SFX 驻留/批量解码，长音乐/语音流式。
- 编码格式通过浏览器能力探测选取，清单提供兼容变体。
- 环境声按地区/时间分层，不下载整区长立体声。
- 声纹玩法提供视觉/触觉等价。
- 语音按行 ID、语言、版本、时长、响度和许可管理。

## 7. VFX 与 Shader

特效先服务读招、命中、证据和环境状态。透明过绘是网页 2D 主要风险之一：限制全屏雾、叠层粒子和大面积半透明，低档减少装饰但保留关键提示。

Shader 数量、编译时间和变体进入构建报告。WebGL2 context 恢复后所有 GPU 资源可由 CPU/包缓存重建；不能把唯一状态存在纹理/FBO。

## 8. 内容分包

```text
shell                 HTML/CSS/JS/Service Worker
runtime               WASM + loader
boot                   字体、错误 UI、序章最小资源
core_shared            通用战斗/UI/音频
region_jiangnan_*
region_longquan_*
region_huizhou_*
locale_zh_cn
quality_standard/high
```

Boot 不预载三地区。子地区进入前后台下载依赖，安全枢纽显示真实进度。哈希 URL 永久缓存，新 manifest 切换版本；旧版本只在新版本完整验证后清理。

Service Worker 预缓存仅限壳和最小启动资源。地区包按玩家进度/网络空闲缓存，不在首次访问消耗数百 MiB。

## 9. 下载与启动预算

F1 起始目标（Brotli/实际传输口径）：

| 项目 | 目标 | 硬门/说明 |
| --- | ---: | --- |
| HTML/CSS/JS Shell | ≤ 1 MiB | 不含 source map |
| 主 WASM | ≤ 12 MiB | 超出需模块/依赖审查 |
| Boot + 首段内容 | ≤ 22 MiB | 与 Shell/WASM 合计首访 ≤35 MiB |
| 冷启动可交互（20 Mbps） | ≤ 8 s | P95 测试网络；显示分阶段进度 |
| 回访缓存启动 | ≤ 3 s | 不强制联网等 manifest 超时 |
| 单个子地区增量包 | 目标 ≤ 25 MiB | 按真实路线/弱网验证 |
| 单语言语音包 | 独立下载 | 不阻断无语音基础体验 |

Source map、调试符号和高质量源资产不进入玩家 CDN 路径。

## 10. 帧与内存预算

桌面推荐档 1080p/60 FPS；低档 30 FPS。Gameplay 始终 60 Hz。

| 项目 | P95 目标 | 硬门 |
| --- | ---: | ---: |
| 总可见帧 | ≤ 16.7 ms | 连续长帧必须诊断 |
| Gameplay Tick | ≤ 3.5 ms | ≤ 5 ms |
| Axmol 更新/提交 | ≤ 4 ms | ≤ 6 ms |
| DOM 更新 | ≤ 2 ms/批 | 不允许每帧大树 diff |
| 单次内容提交 | ≤ 4 ms | 不出现 >100 ms 长任务 |
| 活跃 WASM heap | ≤ 512 MiB | ≤ 768 MiB |
| JS heap（壳/UI） | ≤ 96 MiB | ≤ 160 MiB |
| 估算 GPU 纹理 | ≤ 384 MiB | ≤ 512 MiB |
| 自动存档主线程 | ≤ 2 ms | 序列化/IndexedDB 异步 |

浏览器实现和设备差异大，报告必须注明浏览器、版本、硬件、DPR、质量档和是否 Pthreads。

## 11. 场景起始预算

- 同屏逻辑敌人可多于同时进攻者，但斜向全景下同时高威胁者必须由 F1 读招样片校准；F1 正式段沿用最多 2 个高威胁者，压力场景上限暂为 12。
- 活跃高逻辑实体 ≤80；群众使用低频/抽象代理。
- 每帧 Axmol draw call 目标 ≤500；同材质/图集合批。
- 可见骨骼角色按平台和屏幕尺寸分更新率。
- 粒子、灯光、遮罩和 RenderTexture 有显式数量/像素预算。
- DOM 常驻可交互节点目标 ≤300；长列表虚拟化。
- 每个 Cell 定义最坏战斗、最快穿越、归位变体和 context restore 场景。

这些数值由 F1 压力样片校准，未校准不等于无预算。

## 12. 内存与生命周期

WASM32 线性内存增长不一定能无代价回收给浏览器，因此从启动就重视峰值和碎片：

- 地区 arena/对象池在离开时整体重置。
- 大型解压缓冲复用并限并发。
- 图集上传完成后按策略释放 CPU 副本，但 context restore 所需数据可从 Cache/包重读。
- 资源引用图和所有权可视化；未使用包/Cell 按 LRU 与安全水位卸载。
- 30–60 分钟循环游玩后回枢纽，WASM/JS/GPU 指标应回到稳定平台线。
- 页面隐藏停止渲染/音频和非必要预取，不在后台膨胀队列。

## 13. 质量档与多端

Standard/High 控制 DPR、背景层、阴影/光效、粒子、动画更新率和纹理。浏览器默认不直接使用物理 4K 内部分辨率；按 GPU/屏幕/帧率选择渲染比例。

Windows 原生可增加更高资源档，但内容/碰撞/玩法不变。移动端使用独立图集/音频/触控档，必须从同一源资产生成，不能手工维护“手机版图”。

## 14. Git LFS 与许可

Git LFS 跟踪不可再生源资产和大二进制；可确定生成的图集/缓存不进 Git。启用前锁定托管配额、备份、客户端版本和文件锁策略。

每个资产有 `license.yaml`。字体、音频、Spine Runtime/编辑器、素材包和 AI 生成资产必须在构建时产出许可报告；未确认的参考图不进入发布包。

## 15. 性能工作流

1. 模板选择 Web 性能档。
2. 内容工作台实时显示下载、解压、WASM/JS/GPU 内存和 draw call。
3. 每子地区维护冷缓存、热缓存、弱网、最密战斗、快速穿越、长时回环六条路线。
4. CI 在固定浏览器/硬件跑 trace 和回放，比较趋势。
5. 超预算先改生命周期、包边界、图集/合批和设计，再微优化 C++。
6. 优化后复测可读性、DOM 辅助和 Native 对照。

发布保留每地区 Web 性能报告、网络瀑布、缓存状态和长时内存曲线。

## 16. 视觉方向冻结边界

1.0 基线是“有材质重量的东方幻想 2.5D 斜向全景”，不是纯侧视横版、自由旋转写实 3D、严格像素画，也不是对《斗战神》或《暖雪》的视觉复刻。第一指导《斗战神》约束构图、比例、空间层次、材质和场面调度；第二参考《暖雪》只约束缩放后的轮廓与读招。人物比例、服饰、器物、建筑、色彩、动作和 UI 必须形成《天工渡》自身语言。

- 角色轮廓先表达职业/武器/动作方向，再添加纹样和工艺细节；缩小到战斗尺寸仍能辨认。
- 环境以地面走位面、纵深通路、高度/桥层、危险区、可交互工事、前景遮挡和远景叙事分层；高细节背景不能吞掉脚点与读招，遮挡淡化不能暴露本应隐藏的信息。
- 工灯证据使用“材料/受力/工序”的形变、线向、纹理和声光，不统一涂成魔法荧光。
- UI 使用温纸、墨、木、金属与灯色；可读文本在干净底层上，仿旧纹理只作边缘装饰。
- 江南主色倾湿润青灰、靛蓝、油纸暖色；龙泉为铁灰、炉红、矿砂金；徽州为墨黑、石白、松绿。关键敌我/危险色不随地区语义漂移。
- 真实工艺参考需登记来源；概念图只作方向，不把未授权参考直接描摹进资产。

正式风格在 G2 前用同一“雨夜试伞”斜向全景构图制作 2–3 套小样，至少同时出现前景遮挡、中景纵深战斗、远景工艺地标、玩家、两类敌人与一个证据点；按《斗战神》第一指导的一致性、读招、脚点、角色识别、生产成本、压缩后画质和多端缩放选择。选择后记录为美术基线，不能让每地区变成不同画风。

## 17. F1 最小资产集合

F1 只生产足以测试管线和性能的代表样本：主角一套灰盒/近最终轮廓、沈砚一套核心 NPC、漏雨伞偶和褪色纸鹭两个敌人家族样本、四时伞祟阶段样本、镇口/作坊两个斜向全景 Cell kit、前中远景与遮挡/淡化样本、基础 HUD/字体、雨/风/证据/VFX、一段环境音乐和必要 SFX。

每项至少有 source、import、license、runtime、preview 和预算记录。可以使用占位音画，但必须清楚标记、可由锁定工具替换，并禁止携带未授权网络素材进入对外构建。F1 用这组样本校准：角色屏占比、地面单位/高度→斜向屏幕映射、纵深排序、遮挡透明度、Sprite/骨骼/预渲染/实时 3D 组合、图集/网格与材质、动画方案、透明过绘、音频格式和单子地区包上限；校准前不批量制作 24 名核心 NPC 最终动画。

### 17.1 台账定位与状态

本节是 F1 美术资源需求与落地状态的唯一台账；它记录“需要什么、为什么需要、由谁负责、怎样接入和怎样验收”，不替代 DCC 源文件、资产 metadata、Git LFS 清单、具体排期或发布包 manifest。代码绘制轮廓一律记为 `Placeholder`，不得因浏览器中已经可见而把正式美术标为完成。

状态统一为：

- `Missing`：只有需求，没有可审查源资产。
- `Briefed`：方向、比例、动作/构图合同、预算和参考来源齐全，可以开工。
- `Placeholder`：运行时已有程序轮廓或临时代用品，只能验证接口与可读性，不能代表正式美术完成度。
- `Blockout`：已有原创灰盒/轮廓，可进入同镜头比较，但不可当正式资产。
- `Review`：源资产、导入和运行时变体齐全，等待美术/玩法/技术验收。
- `Integrated`：已通过锁定管线进入 Web/Native Preview，尚未通过完整路线。
- `Validated`：绑定 build ID，在代表镜头、动作、遮挡、性能和许可门通过。
- `Deferred`：明确不进入 F1，保留 Owner、原因和后续门。

每次状态变化必须补齐证据位置和最后验证 build；“口头完成”“只有导出 PNG/GLB”“只在 DCC 中能看”都不能升为 `Integrated` 或 `Validated`。

### 17.2 F1 美术资源需求台账

| 需求 ID | P | 资源组 | 当前状态 | F1 最小交付 | 接口与验收 | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| `ART-F1-VIS-001` | P0 | 视觉基线构图板 | Briefed | 同一雨夜试伞镜头 2–3 套原创方案，含前景遮挡、中景纵深战斗、远景工艺地标、玩家、两敌与证据点 | 《斗战神》第一指导构图/尺度/材质/场面调度，《暖雪》仅作缩放读招第二参考；记录来源与否决原因 | 美术指导 |
| `ART-F1-SCALE-001` | P0 | 角色/场景比例尺 | Missing | 玩家、沈砚、伞偶、纸鹭、四时伞祟、门/伞架/工位在 720p、1080p 与当前 1230×692 验收画幅的屏占比表 | 脚点、碰撞原点、地面高度、遮挡层和 Boss 尺度与量化世界单位一致 | 技术美术负责人 |
| `ART-F1-CHAR-001` | P0 | 玩家“执灯渡人” | Placeholder | 原创轮廓、正背/斜向视图、檐守/翻花两架势、移动/纵跃、轻重击、守势、闪避、受击/倒地最小动作集 | 动画只表现 Gameplay Tick；统一脚底锚点、受击盒原点和武器接触标记，替换占位不改命中规则 | 角色美术 + 动画 |
| `ART-F1-NPC-001` | P0 | 沈砚核心 NPC | Placeholder | 纸伞师父轮廓、待机、教学指引、调查回应、结算回应与最小表情/手势集 | 对接未来 `template_core_npc`；身份、日程、关系和对白不写入贴图/动画名 | 角色美术 + 叙事动画 |
| `ART-F1-ENEMY-001` | P0 | 漏雨伞偶家族 | Placeholder | 地面型轮廓、受潮油纸/伞骨材质、预警、攻击、格挡反馈、破势、受击与退场样本 | 与纸鹭在战斗尺寸下轮廓/高度/运动语法不同；Home Pose、Formation Slot 和命中窗来自内容/Gameplay | 敌人美术 + 动画 |
| `ART-F1-ENEMY-002` | P0 | 褪色纸鹭家族 | Placeholder | 高位纸构件轮廓、折叠/展开、俯冲预警、攻击、闪避反馈、受击与退场样本 | 高度和纵深可读，不靠颜色单独区分；不得继续把训练构件当正式敌人资产 | 敌人美术 + 动画 |
| `ART-F1-BOSS-001` | P0 | 四时伞祟 | Placeholder | 统一本体与春/夏/秋/冬四阶段轮廓/材质/VFX变化、阶段预警、受击、归位与制伏样本 | 75/50/25/0 阶段由 Gameplay 驱动；每阶段至少两种非颜色通道可辨，Boss 尺度符合斜向全景 | Boss 美术 + VFX |
| `ART-F1-ENV-001` | P0 | 雨渡镇口 Cell kit | Placeholder | 渡口地面、泊位、檐棚、护牒检查、引路灯、工坊铃、远景水面/地标、前景框景 | 支持斜向地面走位、脚点、遮挡淡化与 6 节点准备链；远景不参与权威碰撞 | 场景美术 |
| `ART-F1-ENV-002` | P0 | 伞巷/雨障 Cell kit | Placeholder | 伞巷地面与纵深岔路、破棚缝、积水槽、纸鹭诱饵、可选路线、前中远景和遮挡体 | 支持双伞偶→雨障→纸鹭流程及选择分路；交互锚点和碰撞由 Definition 驱动 | 场景美术 + 技术美术 |
| `ART-F1-ENV-003` | P0 | 共用工位/返程 Cell kit | Placeholder | 春冬工痕、共同账页、校准架、三角接敌区、增援落点、返程捷径与状态反馈 | 支持选择驱动的不同调查/增援/动作要求；资产不能保存第二份任务状态 | 场景美术 + 道具美术 |
| `ART-F1-ENV-004` | P0 | 四时庭/Boss 场与结算回程 | Placeholder | Boss 战地面、四季环境差异、门前安全点、制伏/归位两类结算构图和返沈砚路径 | 受控镜头不越过玩法真相；失败重试、结算选择和返回路径保持可读 | 场景美术 + Boss 美术 |
| `ART-F1-PROP-001` | P1 | 工艺调查/交互道具组 | Placeholder | 护牒、缆绳、灯、铃、棚缝、积水槽、诱饵、春冬工痕、账页、校准架、捷径机关 | 每件带 Stable Asset ID、脚点/操作点、状态变体、source/import/license/runtime/preview/budget metadata | 道具美术 |
| `ART-F1-ANIM-001` | P0 | 通用动作语法与事件标记 | Missing | anticipation/contact/recovery/loop、脚底锚点、武器接触、受击方向、退场标记规范及代表动作 | 导入报告与 Ability Tick 比较；动画帧不直接判伤，不以表现事件推进任务 | 动画负责人 + 战斗负责人 |
| `ART-F1-VFX-001` | P0 | 战斗/工灯/天气 VFX | Placeholder | 雨风、轻重击、格挡/破势、闪避、受击、工灯证据、增援入场、四季阶段与归位样本 | 限制透明过绘；关键危险至少两种通道；减少闪烁/减震模式仍保留语义 | VFX + 技术美术 |
| `ART-F1-UI-001` | P1 | F1 HUD/交互/选择/结算视觉 | Placeholder | 资源、姿态、敌人预警、任务、交互、选择、Boss、失败重试、奖励未保存状态与双结算最小皮肤 | DOM/Canvas 分层、文本对比度、键鼠/手柄焦点和战场遮挡预算通过；UI 不持有玩法真相 | UI 美术 + UX |

音频、配音与字幕事件由 `F1-AUD-01` 维护；本台账只记录需要与画面同步验收的声音依赖，不把音频生产状态混入美术状态。

#### 17.2.1 一次性视觉与玩法样例（2026-07-12）

[`prototypes/f1-visual-sample`](../../prototypes/f1-visual-sample/README.md) 已把雨夜伞巷方向稿落成一场可操作的浏览器精英战，用于校准《斗战神》第一指导下的斜向全景镜头、角色屏占比、前中远景、HUD 占位、轻重攻击、架势切换和闪避读招。方向图、预渲染背景、单张角色切图及程序 VFX 均是项目内生成的 `Placeholder`；样例没有 source/import/license/runtime/预算的正式交付链，也没有动作序列、遮挡网格或正式内容，因此 `ART-F1-VIS-001` 仍为 `Briefed`，角色、敌人、环境和 UI 项也不得据此升级状态。正式制作只可把样例中的构图和屏占比作为下一轮 brief 输入，不得直接把样例资产接入出货包。

### 17.3 单项资产接入清单

任一台账项从 `Blockout` 进入 `Review` 前必须同时提供：

1. 原创 source 与可重建导出配置，包含作者、工具/版本和许可来源。
2. Stable Asset ID、拥有包、目标平台变体、依赖与替换的 Placeholder ID。
3. 运行时格式、锚点/轴向/单位、图集或网格/材质、状态/动作清单与预算估算。
4. 当前 2.5D 斜向全景代表镜头截图；角色/敌人还需战斗尺寸、遮挡、脚点和预警截图。
5. Axmol Web Preview 与 Native Preview 的导入结果；若 Native 尚未进入该门，明确标为待验证，不可写“跨端完成”。
6. 性能/内存/下载、透明过绘或骨骼更新成本，以及低档/减少动态效果替代。
7. 与 Gameplay/任务/存档的边界说明：哪些是视觉状态，哪些稳定状态必须来自只读 Snapshot。

技术美术负责人每个阶段节点更新状态；美术指导签视觉，战斗/关卡签可读性，QA 绑定 build 证据，许可负责人签来源。需求变化先更新本台账和受影响预算，再进入资产制作；不得用已经投入工时倒逼 Runtime 接受不兼容格式。

## 18. 动作与命中表现合同

每个战斗动作的源资产标记 anticipation/contact/recovery/loop 视觉帧和脚底锚点，但 Gameplay 的 startup/active/recovery Tick 是唯一规则。导入报告比较视觉接触点与规则命中窗，偏差超阈值阻断预览；修动画或数据，不能在 Presentation 偷补伤害。

命中停顿、相机、闪白、音效和震动由 `FeedbackTag` 组合，并有减少闪烁/减震/静音替代。Boss 阶段关键读招至少使用轮廓/运动、声音/字幕和地面/环境中两种通道；颜色/音高不能是唯一答案。
