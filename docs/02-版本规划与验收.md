# 02｜版本规划与验收

> 状态：Active，持续更新
> 只记录尚未完成的计划、当前门禁和验收条件；已完成事实移入 03
> 最后更新：2026-07-14
> 维护者角色：制作人/游戏总监；技术负责人和 QA 负责人共同维护门禁证据

## 1. 当前阶段

F0.1 技术修订、`F1-DEV-01` 构建/宿主与 `F1-DEV-02` Session/输入/回放已经完成并记录在 03。`F1-DEV-03-RWD-01` 正在分支 `主开发` 上收口结算奖励原子事务；底层事务与应用接线候选已分别形成 `8bec860`、`5db30a7`，`a9ac2fa` 已绑定正常三浏览器、第二结算离线和 Head CAS 冲突证据。奖励事务中断后的显式重试与远端阶段门仍未完成，因此状态保持 In Progress，不得写成 Verified。当前主开发流仍处于 **F1 G1–G2 实施：`F1-GAME-01` 雨夜试伞玩法灰盒**。玩法必须直接消费既有 60 Hz Session、Action/命令、内容 Definition 与只读快照边界；不得在 JavaScript、表现层或存储层复制玩法真相。

首两拍正式集成按五个可回滚阶段推进。Stage① 已建立通用 `Definition → QuestUiProjection` producer 与精确选择意图校验；Stage② 已在主开发分支形成 Web ABI 1.2 的 projection/selection/close 信封、Platform 活跃 choice 身份与 wire sequence 门、正式 DOM Presentation/Action consumer，以及无 Web consumer 时可见且不默认首项的 Native 数字选择 fallback。UI 只提交绑定 projection sequence/checksum、Objective、interaction 与 selection 的意图；Promise resolve 不本地隐藏面板，只有更新的权威事件或精确 close acknowledgement 才能替换/关闭。attempt classification 由 Definition 精确 evidence rule 派生并只供 QA/遥测，不进入 renderer model、玩家文案或 Action。Schema 1.6.0 的八个 cue / 14 条规则现已接入：抵达线索、系泊方法、训练道三个真实 choice 通过正式 Action/renderer 或 Native fallback 消费，固定两路线 UI checksum 为 `955f3abb832aa33f` / `45f516efd2d8c92d`；应用仍按通用 `has_authored_cue` 分流，后五 Beat 的路线、调校与结算 choose 保留具体世界交互路径并由全 7 Beat/双结算测试覆盖。其余 11 类真实 raw-result emitter、10 个 Stable Asset ID resolver、训练安全点 Encounter+Session+Combat 重建、同一玩家构建浏览器路线和 P01–P05 真人证据仍是后续门禁，不得把本阶段写成首两拍完整集成或 Verified。

固定 60 Hz Session、地面纵深输入/碰撞、量化快照与 Native/Web 黄金回放子门已经成立；Guest 首次保存、事务后真实 UI 状态和刷新恢复此前也已由 `g1-gh-29161595494-1` 在三浏览器验证。当前奖励候选由纯 C++ `DeterministicQuestResolutionRewardResolver` 给出稳定 `sourceId/rewardId/rewardDedupKey`，经 `IF1RewardClaimSink` 进入 `ProfileProgressCoordinator`；Web ABI 1.1 把规范 80-byte Persistent Operation 附在快照后，IndexedDB 在 `snapshots + operations + profile_heads` 同一事务中执行 Operation `add`、快照 `add` 与 Head CAS，只有 `transaction.complete` 后才通知表现层。精确候选 `a9ac2fa` 已通过 Node 35/35、MSVC/Clang Release 各 15/15、Emscripten Release、Chrome/Edge/Firefox 正常矩阵、Chrome 排水/冬肋/直接制伏离线结算和 Chrome Head CAS 冲突；两种结算、首次/重复/非法 claim、刷新恢复、Operation 数、UI 提交前后与现有玩法/回放均已绑定本地报告。仍缺 `reward_abort_once` 的事务中断后显式重试证据。Draft PR #1 已触发 GitHub Actions run `29268579868`：设计/架构/锁定、Native 四配置与 Web Single 双配置通过，Chrome/Edge 完整矩阵通过；Firefox 唯一失败在第 6 Beat 结算前的 QA 移动墙钟超时，当时任务未结算且 Operation 数为 0。已上传 artifact digest `7250fd8af506fa9d20fda5893e7ef911a8d1a9f50d210ea904cc89ec78532b2d`，但该 run 使用 PR merge SHA `576b2bd` 而非精确 head，Firefox 重跑/复现与 exact-head 远端证据仍待关闭，故 `F1-DEV-03-RWD-01` 不能标记 Verified。玩法侧全部 7 Beat、双路线、返程 3→4、四季 Boss 与双结算继续成立，但两条快跑仍只有 6035/6224 个合格 Tick且均为 0/7 Beat 达标；`37f5dd6` 已建立通用复合条件的固定容量 C++ 合同/只读求值器 Bootstrap，内容 Schema/生成器/消费者接线与真实多 Objective 会合仍缺失。Combat Resolver 与 Encounter Director 各自的多 Actor `reinforce` 批量提交、非法批次无部分变更和相同 sequence 回放拒绝，已由 MSVC/Clang Release Native 用例证明；但 F1 内容仍只编排单 Actor 增援，`F1GrayboxLayer` 也尚无跨 Director+Combat 的 prepare/commit/rollback，因此不能宣称多单位增援端到端完成。Encounter Director 已新增有限职责、显式 Actor 绑定、1–600 Tick 令牌租约、跨职责轮转、击败归还和 checksum 状态；压迫者/侧击者双令牌、骚扰者补位、非法半配置与 60/30 FPS 固定步一致性已由 MSVC/Clang Native 证明。当前 F1 Definition 仍固定为 legacy 1 令牌，职责/租约尚未进入 JSON Schema、生成器、生成头或 F1 内容，Director 租约也尚无可恢复快照/回放协议；两令牌浏览器路线与压力调平仍缺失。完整跨域事务快照、真实 Cell/资产和真人 60 分钟盲测同样未完成。因此 **G1 仍未整体通过**，GitHub/CI 按成果门集中调试，不再阻塞每个玩法微提交。

其后，`fcd1fbe05f087b8ef2e5dddfaef1fd6575adf582` 将六项敌群激活继续扩为逐 Actor Home Pose/Formation Slot 合同：训练单体、伞巷 1/5 对向夹击和返程 0/3/6 三角包围由同一 JSON/Schema→C++ 链生成，并由 Combat 与 EncounterDirector 在同一安全点应用。该精确提交同样通过 Node 33/33、MSVC/Clang 各 14/14、Emscripten Release 与 Chrome 七段全流程；报告 `build/browser-qa-formations-final-chrome-fcd1fbe/report.json` 中返程额外重试为 0、结算合格 5679 Tick、闲置 385 Tick、失败重试 603 Tick、0/7 Beat 达标、三类浏览器错误为 0。动态队形、多令牌协同、导航避障和真正的三种完整战术编队仍未完成；这里只把可批量生产的落位/接敌槽位边界标为 Bootstrap Implemented，仍不改变 `1H PENDING` 与远端阶段门待聚合的结论。

精确提交 `8d8ab76ef6e9214b638b4eb4463f63338ba3d86f` 将伞巷 Beat 从 3 项扩为 6 项 Objective，并把场景交互由 17 项增至 20 项：击败双伞偶后必须沿斜向纵深依次检查破损棚缝、疏通积水槽、升起纸鹭诱饵；三步期间 Active 敌人数保持 0，只有诱饵 Objective 提交后才在 Tick 安全点激活纸鹭，纸鹭解决后才开放路线选择。该提交通过 Node 33/33、MSVC/Clang 各 14/14、Emscripten Release 和 Chrome 全 7 Beat；报告 `build/browser-qa-rainworks-final-chrome-8d8ab76/report.json` 中返程额外重试为 0、结算合格 5445 Tick、闲置 388 Tick、失败重试 545 Tick、0/7 Beat 达标、三类浏览器错误为 0。此处只建立可内容化的有序环境处理链和第二波触发门，不代表可改变世界状态的环境模拟、分支谜题或完整 20–32 分钟段落；F1 继续保持 `1H PENDING`。

上一精确提交 `e3e4d6524399fdeb2042b2c17d2398ad648455fc` 将 F1 激活合同升级为 schema `1.1.0`，以独立 `EncounterActivationCommand` 和 `replace` / `reinforce` 模式把内容激活与倒地重试分开。返程 Beat 新增校准架交互并在 Objective 完成后追加一只休眠纸鹭，入口三角 3 敌变为 4 敌混合组；增援不恢复玩家或既有敌人的资源/位姿，失败重试会按已完成目标重建叠加状态。交互总数由 20 增至 21，激活定义由 6 增至 7。该提交通过 Node 33/33、MSVC/Clang Release 各 14/14、Emscripten Release 和 Chrome 全 7 Beat；报告 `build/browser-qa-reinforcement-final-chrome-e3e4d65/report.json` 的 expected/Host/replay commit 完全一致，返程额外重试为 0、结算合格 8013 Tick、闲置 510 Tick、失败重试 561 Tick、0/7 Beat 达标、三类浏览器错误为 0。这里完成的是单 Actor 叠加增援 Bootstrap，不等于多单位调度、角色职责层、多令牌协同或 43–50 分钟段落已经完成；F1 继续保持 `1H PENDING`。

前一精确提交 `5c24344efaf1edbf5db496476b6f1622c08867c6` 将 F1 合同升级为 schema `1.3.0`，为 `QuestCombatTriggerDefinition` 增加稳定的选择 Objective/Option 门，并把返程 Beat 由 3 项扩为 4 项 Objective。校准架仍按此前选择追加 Actor 106 地面伞偶或 Actor 105 高位纸鹭；新增动作 Objective 随后只接受春肋的檐守重击或冬肋的翻花轻击。两个 Definition 共用同一 Objective，半门、未来门、重复 option、混合不兼容门和覆盖不全均失败关闭；Web 在已接受战斗信号后重新核对敌群结果，避免“先清场、后做动作”软锁。战斗触发定义由 5 增至 7。该提交通过 Node 33/33、设计校验、MSVC/Clang Release 各 14/14、Emscripten Release 和 Chrome 春/冬两条全 7 Beat 路线；报告 `build/browser-qa-return-action-spring-final-chrome-5c24344/report.json` 与 `build/browser-qa-return-action-winter-final-chrome-5c24344/report.json` 的 expected/Host/replay commit 完全一致，返程均由 1/4、4 敌推进到 2/4、4 敌，最终任务解决、奖励收据就绪、三类浏览器错误均为 0。春/冬结算合格 Tick 分别为 5917/5996、闲置 250/277、失败重试 1478/546，返程额外重试为 1/0，均为 0/7 Beat 达标；两张动作截图均为 1230×692、全不透明且无纯黑/近黑像素。这里完成的是“一项先前选择同时改变后续单体增援与动作要求”的 Bootstrap，不是通用布尔条件语言、多 Objective 会合、多单位调度或最终 43–50 分钟段落，F1 继续保持 `1H PENDING`。

最新精确提交 `d6686a5ad634f2fe647735f61f815dfe02cd1f17` 将 F1 合同升级为 schema `1.4.0`，为 `QuestInteractionDefinition` 增加成对的选择 Objective/Option 门。伞巷路线现在有雨棚与排水两个稳定选择；进入共用工位后，同一春痕 Objective 只在对应路线的位置开放，另一入口无副作用。JSON 生成器、C++ Runtime/Resolver 与 Native 负向用例拒绝半门、未来门、重复 option、不兼容混合门和 authored option 覆盖不全；Web Marker/Prop 只表现权威选择匹配的变体。该提交通过 Node 33/33、设计校验、MSVC/Clang Release 各 14/14 与 Emscripten Release。Chrome `150.0.7871.101` 的雨棚/春肋与排水/冬肋两条全 7 Beat 路线分别记录在 `build/browser-qa-lane-canopy-final-chrome-d6686a5/report.json` 和 `build/browser-qa-lane-drain-final-chrome-d6686a5/report.json`：expected/Host/replay commit 完全一致，错误入口均保持 0/4、正确入口推进到 1/4，最终任务解决且奖励收据就绪，回放校验和均为 `fde5024f1286eead`、内容指纹均为 `c566da855537a288`，三类浏览器错误均为 0。两条路径的合格/闲置/失败重试 Tick 分别为 6035/255/1477 与 6224/246/1414，返程额外重试均为 1，仍是 0/7 Beat 达标；四张路线/入口证据均为 1230×692、851160 像素全不透明且近黑异常为 0。这里完成的是“早期路线选择改变后续一个场景入口”的 Bootstrap，不是通用任务分支图、真实双 Cell 流送、可保存环境状态或完整 20–32 分钟伞巷内容，F1 继续保持 `1H PENDING`。

F1 必须用真实浏览器构建证明路线可行。Axmol 官方把 WebAssembly 标为 Preview，因此在 F1 通过前，不批量生产最终资产，也不把“官方列出支持”误当“本项目已经适合发布”。F1 的唯一产品纵切见第 3 节；各组不得另造互不兼容的演示关卡。

自 2026-07-12 起采用“本地快速门持续运行、GitHub 阶段门集中收口”的推进节奏：先完成边界清楚的代码骨架和本地闭环，再在 ABI/存档闭环、可玩灰盒、工具闭环、QA/OPS 候选等阶段节点触发完整远端矩阵。阶段间不为每个微小提交反复追逐托管 runner；但数据损坏、安全、工作流/工具链改动和发布阻断仍须立即验证。该节奏只减少远端往返，不降低受影响模块的本地测试、失败记录或最终门禁。

### 1.1 当前可交接成熟度

当前设计**足够组建团队、估算 F1、分配 Owner 并开始预制作**，但**不足以直接批准三地区全量生产**。下表中“已有”是可依赖的输入，“开工后仍要产出”不能被供应商或内部团队当作隐含需求自行猜测：

| 专业域 | 当前已有，可用于开工 | 尚未完成，不能冒充生产就绪 | 对应首批任务 |
| --- | --- | --- | --- |
| 产品/策划 | 玩家承诺、1.0 边界、F1 流程、内容规模和门禁 | 真实人员容量、成本/档期、商业模式、完整数值平衡与逐关卡节奏表 | `F1-PROD-01`、`F1-GAME-01` |
| 工程 | C++/WASM 分层、精确工具链锁；60 Hz Session、量化回放；Web ABI 1.2/Guest IndexedDB；F1 结算奖励 Operation+Profile 快照+Head 单事务候选及 CAS 冲突回滚证据；F1 内容 Definition、独立目标图会话、固定容量 Quest Condition Program 求值器、Definition 驱动的只读 Quest UI projection/choice intent、Platform envelope/close freshness、DOM renderer/Action 与 Native fallback、1.6 三类真实 choice 正式消费、确定性战斗/遭遇/任务/Boss、Combat/Encounter 各自的多 Actor 增援批量原子边界、有限敌人职责与令牌租约/轮转 Runtime、7 个内容驱动移动安全点、数据驱动恢复、失败重试、敌群隔离与 7/7 Beat Web 灰盒本地门 | 奖励事务中断后显式重试浏览器证据、远端阶段门、其余 11 类 Quest UI 实际 emitter/安全点阶段重建/资产与同一玩家构建浏览器接线、条件程序内容接线/迁移、职责/租约 Schema 与 F1 内容接线、Director 租约快照/回放协议、跨 Director+Combat 激活原子协调、完整 AI、战斗/任务/世界/Profile 跨域事务快照、60 分钟盲测与真实基线设备性能证据 | `F1-DEV-03-RWD-01`、`F1-GAME-01` |
| 工具/内容技术 | Schema/模板目标、稳定 ID、Workbench 范围和 DoR/DoD；F1 schema `1.6.0`、41 项含路线选择门的交互、8 项 Quest UI cue / 14 条 evidence rule、8 项有序且可由选择互斥约束的战斗触发器、3 组稳定选择、7 项安全点、12 项带 replace/reinforce、Placement 与选择门的敌群激活、4 项 Boss 阶段、2 项奖励收据映射及 11 实体/17 能力战斗包的 JSON→C++ 生成合同和内建 Definition Provider | 可使用的角色/Boss/任务编辑器、条件 Program 的 JSON Schema/生成器/内容消费迁移、多单位增援内容编排与 Web 证据、Workbench 条件/动作/分支/编队/时间线预览、通用 baker、迁移、资源预览与包格式实现 | `F1-DEV-04`、G3 内容工具 |
| 叙事/任务 | 世界圣经、三部结构、24 NPC 驱动、18 支线与 F1 双结算 | F1 最终台词/演出脚本、全部任务节点、研究审阅与本地化稿 | `F1-NAR-01` |
| 美术/动画/VFX | 高层风格、地区意象、角色/Boss 功能、资产和性能预算 | 完整视觉圣经、比例/色彩/材质库、模型表、动画/VFX 清单与最终源资产 | `F1-ART-01` |
| UI/UX | 信息层级、输入、焦点、可访问性、DOM/Canvas 和 F1 UX 路径 | 设计令牌、完整组件稿、可点击原型、读屏/手柄实测与本地化压力稿 | `F1-UI-01` |
| 音频/配音 | 世界语气、战斗读招需求、并发/包体与字幕原则 | 音频圣经、事件/总线表、主题动机、配音范围、混音和正式素材 | `F1-AUD-01` |
| QA/发布/运维 | 测试层级、证据结构、性能门、环境/回滚和安全规则；F1 Windows 2022 runner 与三浏览器功能证据已落地 | 基线硬件性能、staging/CDN/监控、回滚/灾备与正式发布演练 | `F1-QA-01`、`F1-OPS-01` |

只有 F1 代表性产物在同一构建中闭环、G3 证明第二个实例可复用、真实团队容量经复审后，才允许 M1 批量扩产。所谓“文档完善”在此阶段是减少猜测并形成可验证任务，不是提前虚构尚未完成的设计稿或代码。

## 2. 路线图

| 阶段 | 核心问题 | 主要交付 | 出口门禁 |
| --- | --- | --- | --- |
| F1 Web 烘焙 | Axmol/Emscripten 能否支撑正式 2.5D 斜向全景动作纵切？ | CMake/WASM、Web Shell、斜向全景代表灰盒、不少于 60 分钟的纵切、IndexedDB、同步 Spike | Chrome/Edge/Firefox 可玩；刷新不丢档；60 Hz；纵深/遮挡可读；加载/内存达标 |
| F1N Native 对照（F1 并行子门，M1 前完成） | 同一 C++ 核心能否无分叉运行？ | Windows x64 对照构建、输入/文件/网络适配 | 同一回放校验和；无 Gameplay 平台特判 |
| M1 江南地区 Alpha | 单地区是否真的有 5 小时深度？ | 江南 6 子地区、7 主章、6 支线、5 Boss、8 NPC、伞器完整线 | 315 分钟盲测；离线/刷新/云同步闭环；性能达标 |
| M2 龙泉扩展 | 第二地区能否复用系统和内容工具？ | 龙泉、剑器、锻造编辑器深化 | 90% 以上内容由模板创建；无地区特判进 Runtime |
| M3 徽州扩展 | 关系、契约和跨区状态能否稳定同步？ | 徽州、跨区关系、结局与冲突合并 | 三地区跨设备接续、迁移和冲突 UI 可解释 |
| A0 Alpha | Web 全内容能否从头到尾完成？ | 功能锁定、全流程、PWA/缓存策略 | 0 阻断；主支线可完成；长时内存不失控 |
| B0 Beta | 浏览器兼容、性能和辅助能否发布？ | 内容锁定、优化、本地化、可访问性 | 浏览器矩阵、弱网、存储清理和恢复门通过 |
| RC 1.0 | 静态站点、CDN、API 和回滚能否安全交付？ | 签名清单、哈希包、部署与数据库迁移 | 灰度、回滚、旧 Service Worker、旧存档与多端冲突通过 |

## 3. F1 唯一纵切：雨夜试伞

纵切取自江南 `jn_chapter_02 / 一伞四骨` 的代表性片段，稳定内容范围为：

纵切稳定 ID：`f1_rainy_umbrella_trial`。

```text
雨渡镇口安全点
  → 纸伞作坊街教学 Cell
  → 沈砚（npc_shen_yan）委托/训练
  → 漏雨伞偶 + 褪色纸鹭两类敌人
  → 两处工痕与一次伞骨调校
  → 四时伞祟（jn_boss_umbrella_wraith）灰盒阶段图
  → 证据不足/完整归位两类结算
  → 作坊路线、沈砚对白和天工录条目变化
```

它是正史内容的灰盒摘录，但运行在 `prototype_f1` 渠道、包集合与 Profile 命名空间；F1 档不直接迁入 1.0 正式档。正史文本可以继续打磨，稳定 ID、玩家动词与验收意图保持；未经实现证明的状态边界可在 G2/G3 通过合同、迁移和夹具评审修订，不能由表现层私自改变。

起始夹具 `fixture_f1_rainy_umbrella_start` 直接构造原型安全状态，不向正式档伪造“第一章已完成”：

| 项 | 起始状态 |
| --- | --- |
| 地点/安全点 | 雨渡镇口至作坊入口；傍晚稳定降雨 |
| 玩家资源 | 生命/体力/定力各 100；工灯势 40；空证据槽 |
| 基础动作 | 地面八向移动、纵跃/跨越、交互、轻/重击、守势、闪身 |
| 武器/工灯 | 训练伞器；檐守/翻花；基础照见和本次结算所需的教学定印 |
| 人物/任务 | 初见沈砚，关系阶段“试手”起点；`jn_chapter_02` F1 摘录任务未接受 |
| 背包 | 1 个教学维修物、无关键剧情外物；货币/经济不参与纵切 |
| 世界 Flag | 仅开放 F1 路线与教学权限；不写 `jn_chapter_01` 正式完成 Flag |

机器口径：`playableTargetMinutes = 60` 指从雨渡镇口获得控制到任一归位结算的首玩目标下限，不含标题/加载、失败重试、挂机或结算后的持久化验证；`endToEndTestBudgetMinutes = 70` 额外包含返回、保存、刷新和离线重开。不得靠重复遭遇、强制等待或降低移动效率凑足一小时。

| 首玩分钟 | 玩家行为 | 必须证明的系统 | 成功证据 |
| ---: | --- | --- | --- |
| 0–3 | 标题页能力探测，以 Guest 新建档并进入 | Web Shell、加载进度、DOM 焦点、单线程 WASM | 冷启动 trace；无账号也可玩；本段不计入 60 分钟玩法 |
| 3–12 | 在雨渡镇口八向移动、沿纵深绕行、纵跃，完成护牒与可选环境交互 | 斜向全景控制、深度碰撞/排序、受控相机、输入上下文、字幕/提示 | 键鼠/手柄均可；脚点/遮挡可读；失焦不粘键 |
| 12–20 | 与沈砚交谈并安全试用檐守/翻花，完成两种防反练习 | NPC 状态、对话、教程、伞器两架势、练习目标 | 跳过/重复教程不破坏任务；练习不发正式奖励 |
| 20–32 | 穿过作坊街第一段，处理双伞偶、三步雨障和诱饵触发的纸鹭，再选择一个可选绕行 | 兵击、定力/破势、内容化环境操作、Encounter 定义、Cell 流式 | 正常/受伤/操作/绕行均可继续；雨障不靠等待推进，同组敌人不原样重复 |
| 32–43 | 照见两处工痕、复核共同工位账并选择一次伞骨调校 | 工灯、证据、工艺、任务图、DOM 面板 | 错误假设可撤销；证据顺序变化不死锁；面板焦点恢复 |
| 43–50 | 经雨棚工位进入第二战斗段，在敌群压力下启动校准架并应对叠加增援，验证调校结果与捷径回环 | 第二遭遇布局、内容驱动增援、短回环、安全点、资源恢复边界 | 两种架势均有用途；增援不重置既有战斗；捷径不是跳过全部证明的调试门 |
| 50–60 | 对战四时伞祟灰盒的春/夏/秋/冬阶段 | Boss 阶段、天气/场地、失败重试、辅助 | 3 次以上机制变化；门前重试不重播长段；低证据仍可完成 |
| 60–63 | 直接制伏或以证据归位，返回沈砚处观察即时变化 | 选择、奖励幂等、关系/记录/世界状态 | 从 3 分钟获得控制起已达到至少 60 分钟；两条结果各自可继续 |
| 63–70 | 返回作坊安全点，保存、刷新、离线重开 | IndexedDB、包缓存、Session 恢复 | 回到合法安全点；不重复发奖；本段属于端到端验证而非玩法凑时 |

当前本地 Web 构建已能直观看到代码绘制的雨夜斜向全景，并用 `WASD/方向键`、`F`、`Space`、`J`、`K`、`Shift`、`C`、`1/2` 连续操作全部 7 Beat。返程入口为 3 名 Active 敌人；启动校准架后，Gameplay 根据此前春/冬肋选择唯一追加地面伞偶或高位纸鹭并增至 4 名，随后要求春肋檐守重击或冬肋翻花轻击，正确动作把返程 Objective 从 1/4 推进至 2/4，全部清场后才验证调校并开放捷径。`reinforce` 保留玩家和既有敌人的资源/位姿，倒地重试按触发边界去重重建已选状态；动作门同样只读取 QuestRuntime 的权威选择。Boss 仍由内容节拍激活并按 75/50/25/0 生命阈值换相；结算解析器返回稳定奖励收据，Web 先显示 `PROFILE COMMIT PENDING`，仅在奖励 Operation、Profile 快照和 Head CAS 的 IndexedDB 事务完成后显示 `REWARD CLAIM PERSISTED`。7 个 authored safe point 与 `SafePointRetryCommand` 仍只覆盖移动位姿和既有任务进度；奖励账本提交不是战斗 Actor、任务、世界与 Profile 的完整跨域事务快照。HUD 的 `AUDIT Q / IDLE / RETRY / TARGETS` 只读 C++ 审计快照，拒绝挂机、失败和跨 Beat 补时，但不能判断内容是否真正非重复。入口产物为 `build/web-release-single/dist/web/tiangongdu-f1.html`（必须经本地 HTTP 服务访问）；七段功能路径与选择后果成立仍不等于 60 分钟纵切，真实内容密度/长时资源循环/盲测、多单位增援内容与跨子系统提交、职责/令牌调平、正式输入、完整 AI/导航/自主脱战和跨域事务快照均不能跳过。

沈砚训练现通过 5 个内容驱动触发器验证两套架势的核心动作，而非把按键本身当任务进度：重击 Ability 起手、格挡结果、姿态切换、轻击 Ability 起手和闪避结果必须按 Objective 前置链依次发生；格挡完成还会在同一 Beat 把伞偶构件完整替换为纸鹭构件。两轮链只提高操作教学与内容合同密度，不改变 8 分钟目标；当前自动功能路线仍会快速通过，因此还需加入空间推进、多单位/复合条件波次、资源压力和叙事反馈，并由真人盲测证明时长与非重复性。

伞巷雨障处理同样由 `QuestInteractionDefinition` 的稳定 ID、位置、半径和 Objective 前置驱动；Presentation 只画交互锚点和反馈。当前三步会改变任务与敌群激活时机，但尚未写入可保存的世界机关状态，也没有水流模拟、失败替代、多个解法或 NPC 反应；后续扩充必须继续走 `IQuestRuntime` / 内容 Definition，并在引入可持久世界变化时通过 `ISnapshotContributor` 与 Persistent Operation 进入跨域事务，不能在 Web Shell 里追加第二份谜题状态。

### 3.1 当前缺失内容与接口预留

下列内容按事实区分 **Bootstrap Implemented** 与 **Missing/Reserved**。预留代码名用于先稳定依赖方向；只有已经进入代码并通过契约测试的最小边界才可标为 Bootstrap Implemented，且不得把地区、Boss 或 DOM 特判写进 Runtime：

| 缺失内容 | 首轮纵切最低产出 | 预留接口/边界 | 当前状态 |
| --- | --- | --- | --- |
| 世界与 Cell | 雨渡镇口、作坊街两段、雨棚工位、Boss 场、回程安全点；含捷径和遮挡层 | `IContentDefinitionProvider` 提供烘焙定义；`IWorldCellSource` 异步装卸 Cell；Presentation 只消费只读表现快照 | Definition JSON/Schema/生成器、内建 Provider、单场代码绘制斜向全景、量化碰撞/深度排序与前景淡化已 Bootstrap Implemented；真实 5 Cell 几何/遮挡、受控镜头段落和 streamer 仍 Missing/Reserved |
| 玩家战斗 | 檐守/翻花、轻重击、守势、闪身、定力/破势、受伤与失败 | `ICombatResolver` 只处理量化命令/命中；`ICombatEventSink` 向表现层发只读事件；移动通过下一 Tick 权威位姿批次进入命中查询；Action Registry 仍是动作命名真相 | `ICombatResolver` / `ICombatEventSink`、独立生命/体力/定力/工灯/证据、姿态能力、命中距离/高度/楼层、格挡破势、闪避窗口、按实体配置的延迟/间隔恢复、倒地输入冻结、7 个逐段移动安全点、恢复/重试事件、确定性校验和与 Web 键盘意图/反馈已 Bootstrap Implemented；受击硬直/击倒动画、取消窗/连段、正式输入适配及跨域事务快照仍 Missing/Reserved |
| 敌人与遭遇 | 两敌人家族、至少三种不重复编队、脱战/重置/掉落边界 | `IEncounterDirector` 消费 Stable ID 定义并产生命令；敌人 Definition 与 State 分离；`EncounterActorDutyDefinition` 以 Stable Actor Key 绑定有限职责，Encounter 独占令牌池/租约/轮转；`QuestEncounterActivationDefinition` 必须声明 `replace` / `reinforce`、逐 Actor Home Pose/Formation Slot，并可用稳定选择 Objective/Option 声明同边界互斥变体；独立 `EncounterActivationCommand` 不借用失败重试原因 | 伞偶型/纸鹭型两个训练构件、正式伞偶/纸鹭与四季 Boss Definition、全敌对 Actor 默认休眠、训练两轮/伞巷两轮/返程入口与春冬互斥增援/Boss 共 8 项 Tick 安全激活、休眠与 `defeated` 权威分离、单体教学/对向夹击/三角包围三类内容落位、返程选择驱动 3→4 单 Actor 增援、重试去重重建、Active 槽位碰撞与 aggro 范围失败关闭、量化追击/归位、最近有效敌人选择、多 Actor 子系统增援，以及 legacy/有限职责两种最多双令牌 Native 调度边界已 Bootstrap Implemented；职责模式覆盖压迫/侧击/骚扰/控制、1–600 Tick 租约、轮转、击败归还与 cadence-independent checksum。通用条件求值器已独立 Bootstrap，但尚未接入激活 Definition。动态队形、真实多个 Objective 会合、F1 多单位增援内容/跨组件提交、职责/租约 Schema 与 F1 内容、Director 租约快照/回放、刷出/退场演出、复杂感知、导航/避障、F1 两令牌组合与调平、三种完整战术编队、自主脱战生命重置和掉落仍 Missing/Reserved |
| Boss 与任务 | 四季阶段、两证据、共同工位账、调校、双结算、奖励幂等 | `IQuestRuntime` 消费任务图；`DeterministicQuestConditionEvaluator` 只读 Objective/选择事实；`QuestInteractionDefinition` 可用既有选择门声明互斥入口；内容驱动交互/战斗信号/敌群结果/Boss 阶段解析器只选择当前激活目标；`QuestCombatTriggerDefinition` 用稳定 Ability/Stance、Objective 前置与可选的既有选择门描述有序实操；`DeterministicQuestResolutionRewardResolver` 在任务完全解决后只读出奖励收据；应用边界 `IF1RewardClaimSink` 只搬运 claim；真正发奖只产生 Persistent Operation；`ISnapshotContributor` 仍负责未来完整领域快照 | `IQuestRuntime` 已接管全部 7 Beat/目标的唯一真相；沈砚 5 步有序实操、伞巷三步雨障操作、雨棚/排水选择与对应工位入口、调校/结算选择、返程选择驱动的增援与春肋檐守重击/冬肋翻花轻击动作证明、回程清场/捷径、四季 Boss 顺序目标、返沈砚交付和两条稳定奖励/去重收据均已 Bootstrap Implemented。两条收据进入同一 Profile/Persistent Operation 原子提交候选；重复 dedup/刷新/回放不增加 Operation。固定容量后缀条件合同/求值器已 Bootstrap，但尚未进入内容 Definition/Schema/生成器或现有消费者。关系/记录/世界即时变化、通用分支、真实多 Objective 会合、等待/失败替代节点及完整快照贡献仍 Missing/Reserved |
| UI/表现/资产 | 标题、HUD、对话、证据、暂停、结算；玩家/沈砚/两敌/Boss 占位资产及最小动画/VFX/音频 | DOM 只发 `UiCommand`/收 `UiEvent`；`IPresentationEventSink` 与 `IAssetResolver` 不拥有玩法真相 | Axmol 代码绘制玩家/NPC/两伞偶/纸鹭/Boss 轮廓、雨夜分层、23 项场景交互锚点、互斥路线入口、伞巷雨障与返程校准架反馈、工灯调查台、7 Beat 动态任务标题、Boss 季相光环/血量/姿态、双结算/返程提示、奖励收据状态、任务/选择/战斗诊断 HUD、攻击轮廓波、受击缩放和只读事件文字已 Bootstrap Implemented；可访问 DOM HUD、正式结算 UI、可改变世界状态的环境演出、增援刷出/退场演出、动画/VFX/音频、资产解析与镜头演出仍 Missing/Reserved |
| 内容生产与验证 | 上述 Definition、Schema、fixture、bake、负向样例、60 分钟回放检查点 | Workbench/CLI/WASM 共用 ContentCore；`PlaytimeAuditSnapshot` 是 Presentation/QA 的只读计时边界；缺定义、旧版本、非法引用必须失败关闭 | F1 Definition/Schema `1.6.0`、41 项含路线选择门的任务交互、8 个 Quest UI cue / 14 条精确 evidence rule、7 项安全点、8 项含选择门变体的战斗触发器、5 项敌群结果、12 项含 Home Pose/Formation Slot、replace/reinforce 与选择门的敌群激活、4 项 Boss 阶段、3 组稳定选择、2 项奖励收据映射、11 实体/17 能力战斗包、确定性 C++ 生成、两路线三次 choice 投影 checksum 与逐 Beat 时长审计，以及交互/战斗触发/敌群激活/UI 证据的失败关闭合同测试已 Bootstrap Implemented；条件 Program 的 C++ 合同/求值负向测试也已 Bootstrap。多个 Objective 内容会合、其余 11 类真实 UI emitter、条件 Schema/生成器/消费者迁移、多单位增援调度、通用 baker/fixture、Workbench 条件/动作/分支/编队/时间线预览、资源/世界机关预览、正式盲测导出和真正达到 60 分钟的玩法回放仍 Missing/Reserved |

机器合同当前把 `IContentDefinitionProvider`、`ICombatResolver`、`ICombatEventSink`、`IEncounterDirector`、`IQuestRuntime` 标为 `bootstrap_implemented`；Boss 阶段和结算奖励收据解析器属于 Gameplay 的最小确定性实现，不新增第二份端口真相。F1 应用的 `IF1RewardClaimSink` 只把解析器收据提交给 Runtime/Profile 边界，去重和 Operation 所有权仍在 C++ Profile。`IWorldCellSource`、完整领域用 `ISnapshotContributor`、`IPresentationEventSink`、`IAssetResolver` 继续保持 `reserved`；奖励账本候选也不代表完整任务节点库、敌人 AI/导航、完整效果系统或可保存战斗中间帧已经完成。

接口预留的验收原则：第二个 Cell、第二种敌群或第二个 NPC 不得要求修改平台桥、存档格式或专属 Runtime 类；占位内容可替换但 Stable ID、碰撞/命中语义和存档迁移必须保持。

F1 不要求：最终美术、全语音、三地区、完整经济、移动端发布、生产 OIDC、完整云服务 SLO 或 Pthreads 正式上线。占位资产必须可替换且有来源；不能用调试按钮代替玩家路径。

### 3.2 `F1-GAME-01` 当前 Definition of Ready

| DoR 项 | 已锁定输入与执行口径 |
| --- | --- |
| 任务 ID / 标题 | `F1-GAME-01`｜“雨夜试伞”至少一小时玩法灰盒 |
| 单一 Owner / 必审 | Owner：战斗负责人；必审：C++ Core、关卡、表现、内容技术、UX、QA；最终体验 A 为游戏总监 |
| 玩家价值 | 从雨渡镇口获得控制，经探索、教学、两段非重复战斗、调查、四季 Boss 与双结算，形成可独立理解的完整纵切 |
| 权威输入 | 02 §3/3.1、04 §3–6/18–22、06 §7/17–20、07 §3/13.1、11 §9–15/19–22、ADR-0009；机器源为 `f1-vertical-slice.json`、Action Registry 与既有 Session 合同 |
| 稳定 ID / Schema / 合同 | `f1_rainy_umbrella_trial`、5 个 Cell、7 个玩法 Beat、沈砚、两敌人家族、四时伞祟；60 分钟玩法预算与 70 分钟 E2E 预算分离；时间不自动推进任务 |
| 首批交付 | 内容 Provider、纵切会话、斜向投影/深度排序灰盒、玩家移动与两敌占位、两架势最小交叉、证据/调校、Boss 四阶段、失败重试、双结算和安全点快照 |
| 非目标 / 禁止捷径 | 不做最终美术、全语音、完整经济或三地区；禁止 Boss ID 特判、动画帧直接判伤、JS/DOM 玩法真相、调试按钮跳流程、重复刷怪/等待/降速凑时 |
| 自动验收 | Definition 生成一致性/负向校验；MSVC+Clang；固定 Tick/命令/组合快照确定性；目标乱序与幂等；两敌/第二 Cell 不改平台桥；阶段节点再跑 Web Single 三浏览器 |
| 人工 / 性能 / 兼容 | 盲测从控制到任一结算不少于 60 分钟；键鼠/手柄方向、脚点、遮挡、纵深和敌我读招可判；Chrome/Edge/Firefox；最密战斗记录帧时、WASM/JS/GPU 内存与长时回环 |
| 风险 / 回退 / 文档回写 | Axmol Web Preview、混合表现比例和内容产能为主风险；Presentation 可由新 ADR 替换但不改变 GameCore 坐标/玩法合同；每阶段回写 01/02，只有可复验证据进入 03，接口变化更新 Schema/ADR |

## 4. F1 Web 烘焙任务包

### 4.1 F1 硬门

下列链路任一失败都不能进入 M1：

1. C++20 `contracts/runtime/gameplay/presentation/platform` 依赖边界由 CMake 检查。
2. Axmol 2.11.4 LTS 经 Emscripten 生成 Web Single wasm32 + loader，在 WebGL2 下稳定启动、暂停、恢复和销毁。
3. Canvas 仅绘制游戏画面；DOM HUD/菜单能用键盘、手柄、触控模拟和辅助技术操作。
4. “雨夜试伞”含探索、NPC、对话、采集/工艺、普通战斗、Boss、失败重试和结算。
5. 内容按哈希清单分包，首屏只下载壳、WASM、基础 UI 和首段内容；地区资源按需加载。
6. Profile 通过直接异步 IndexedDB 主路径事务保存；刷新、崩溃、离线和浏览器重启可恢复，IDBFS 不作为第二份真相。
7. Native GameCore 测试宿主运行同一自动回放，离散 Gameplay 结果校验和一致；完整 Windows/Axmol 应用留给 F1N。
8. 内容工作台从模板创建 NPC/Boss/任务，并用同一 C++ ContentCore 的 Native/WASM 版本校验与烘焙。

### 4.2 F1 条件/决策 Spike

下列事项必须在 G3 提交实验、风险和书面结论，但“某个增强方案未达到上线门”本身不阻断已经成立的 Web Single + 本地档纵切：

- **Pthreads**：构建最小代表样片，测量收益、COOP/COEP、独立 origin/CDN/OIDC 影响。无显著收益或部署风险过高时，决定 Deferred，Stable 继续 Single。
- **云同步**：锁 Sync IDL/权威矩阵，用 mock 身份验证 Operation 幂等、双端分叉和叙事冲突原型。生产 OIDC、完整服务 SLO 或正式部署可延期，但本地操作日志/导出不能延期。
- **Windows Native 宿主**：G3 只要求纯核心/回放对照；完整 Axmol 窗口、安装和平台体验由并行子门 F1N 放行，必须在 M1 扩产前完成，但不反向阻塞 F1 Web Single 的纵切结论。
- **Safari/移动/PWA 安装**：按第 7 节记录能力和失败原因，不阻断桌面必选矩阵；不得因此宣称这些平台已支持。

若条件 Spike 暴露会破坏既有硬门的基础问题（例如相同存档协议无法表示冲突、Platform 分层无法 Native 编译），该基础问题仍然阻断；可延期的是增强实现，不是合同正确性。

### 4.3 F1 跨团队派工表

下表是交付给各专业组的启动板；能力边界以专业文档为准，排期/优先级/出口以本表为准。每项进入 issue tracker 时必须保留任务 ID、单一 Owner、引用章节、前置、非目标和证据，不得改写成“自行参考文档完成”。

| 任务 ID / 门 | Owner | 权威输入（必须精读） | 首批产物 | 退出证据 |
| --- | --- | --- | --- | --- |
| `F1-PROD-01` 范围与变更控制 / G1 P0 | 制作人 | 00 §1–3/7/12、02 §1–4/12–14、04 §1–2/15、07 §1/12/15–16 | F1 一页范围、人员容量、RACI、非目标、变更/例外模板 | 所有任务只指向同一纵切/Stable ID；每项有 Owner、依赖和可判定出口 |
| `F1-DEV-01` 构建与宿主 / G1 P0 | 技术负责人 | 01 `DEV-CAP-01`、10 §2–5/14/16/20、17 §2–4/7/17、ADR-0005/0006 | CMake/Preset、锁清单、Axmol Web Single、Native Core、架构 lint | 干净 Windows/CI 一键构建测试；启动/隐藏/恢复/context restore 留有日志和 trace |
| `F1-DEV-02` Session/输入/回放 / G1 P0 | C++ 核心负责人 | 01 `DEV-CAP-02`、10 §6–7/17–21、11 §4–8/18–22、Action Registry、ADR-0009 | 60 Hz Session、命令/事件、地面平面+高度碰撞、纵深排序输入、快照与黄金回放 | Web/Native、30/60/144 渲染 FPS 结果一致；八向/纵深、blur、断连、Pause/恢复夹具通过 |
| `F1-DEV-03` Web Shell/存档 / G1–G2 P0 | Web 负责人 | 01 `DEV-CAP-03`、08 §9/14–17、10 §8–10/19、12 §6–9/16–19、17 §3–5/11 | HTTPS Shell、DOM/Canvas 层、WASM 桥、IndexedDB v1、导出/恢复 | Guest 可玩；刷新/崩溃/离线/旧档路径不丢档、不重复发奖、不显示虚假“已保存” |
| `F1-DEV-03-RWD-01` 结算奖励原子事务 / G1–G2 P0 | Web/Runtime 负责人 | 交接 `2026-07-13-f1/01`、10 §19、12 §17–19、16 §12、17 §3/11/17 | 稳定 reward claim、Profile Operation、快照+Operation+Head 单事务、pending/重试 UI、两结算浏览器夹具 | 两结算、首次/重复/非法/刷新/离线、事务中断重试、Head CAS、Operation 数与 UI 前后均绑定精确候选 commit；Native/Web/三浏览器/文档齐全后才 Verified |
| `F1-DEV-04` 人物统一生产链 / G2–G3 P0 | 工具负责人 | 01 `DEV-CAP-04`/§15.1、05 §13/18/20–22、06 §3–6/17–20、11 §3.3/14、12 §2/5–6/9/13、13 §2–5/11–18、14 §3–4/16–18、16 §10 | Definition/State 合同、核心 NPC 模板、角色 Workbench、烘焙/预览/迁移、沈砚与第二 NPC 夹具 | 第二 NPC 无 Runtime 改动；作者 90 分钟内创建/预览/修正；非法引用和旧版本稳定失败 |
| `F1-GAME-01` 雨夜试伞玩法灰盒 / G2 P0 | 战斗负责人 | 02 §3、04 §3–6/18–22、06 §7/19、07 §3/13.1、11 §9–15/19–21 | 移动、伞器双架势、工灯、两敌人、四时伞祟、失败重试和双结算 | 获得控制到任一结算首玩不少于 60 分钟；不用调试按钮或重复刷怪凑时；证据不足/完整归位均可继续且回放稳定 |
| `F1-NAR-01` 任务、对白与归位内容 / G2 P0 | 叙事负责人 | 05 §4–15/18–22、06 §6/8/11/17–19、07 §3/14、04 §10–11、08 §6/10–11/16 | 沈砚关系弧摘录、任务/对白图、两处工痕、伞骨调校、双结果与世界响应表 | 文本、任务 Flag、天工录、NPC 对白和路线变化一致；跳过/重复/失败不死锁 |
| `F1-ART-01` 视觉基准与代表资产 / G1–G2 P0 | 美术指导 | 00 §1.0、05 §2–3/10/18–22、07 §3/6/13–14、14 §1/3–5/7/16–18、04 §3/6/20、08 §3–5、ADR-0009 | 维护 14 §17 美术资源需求台账；交付《斗战神》第一指导的斜向全景构图板、角色/场景比例尺、江南形状与材质语言、2D/3D 混合样片、沈砚/玩家/两敌人/Boss 轮廓、动作表、遮挡/命中/VFX 基准 | 台账 P0 项有 Owner/source/import/license/runtime/preview/budget；同一雨夜 Cell 证明纵深、脚点、遮挡、读招、敌我、交互和证据点；替换占位不改玩法碰撞 |
| `F1-UI-01` 端到端 UX 与 DOM UI / G1–G2 P0 | UX 负责人 | 08 §1–17、04 §3/18、10 §8/21、Action Registry、16 §6/15 | 标题/加载/HUD/对话/证据/暂停/结算可点击流程、组件状态和焦点图 | 键鼠/手柄/触控模拟/读屏可走核心路径；Tab/Escape/IME/失焦行为与注册表一致 |
| `F1-AUD-01` 音频可读性集 / G2 P1 | 音频负责人 | 05 §2–5/10–14、04 §3–5/22、07 §13.1、14 §6/18、08 §8.2、16 §15 | 环境声景、敌/Boss读招、格挡/破势/工灯层级、字幕事件、混音/并发预算 | 关闭画面提示仍能区分关键危险与归位窗口；静音/字幕/动态范围设置可验证 |
| `F1-QA-01` 自动门与证据包 / G1–G3 P0 | QA 负责人 | 02 §3/7–13、01 `DEV-CAP-08`、16、17 §17–19、18 §15 | 测试矩阵、fixture 注册、CI 门、性能/内存基线、录屏/trace/缺陷/例外模板 | 证据绑定 commit/build/环境；从标题页复现；负向、刷新、离线、回滚和辅助路径覆盖 |
| `F1-OPS-01` 环境、渠道与回滚 / G1–G3 P0 | 构建与运维负责人 | 10 §20、17 §2–10/14–20、18 §5/10–13、16 §12/16/18 | CI runner、HTTPS staging、缓存/渠道、SBOM、签名/密钥入口、回滚演练 | 干净 runner 可复现；旧 Worker/旧包/失败发布可回退；秘密不进仓库和前端产物 |
| `F1-SYNC-01` 同步冲突 Spike / G3 P1 条件 | 后端负责人 | 01 `DEV-CAP-07`、12 §9–14/17–19、15 §3–7/15–19、17 §14/19、18 §7–10 | Sync IDL、mock OIDC、幂等/分叉原型、冲突包、DDL唯一约束 | 同请求重试无重复权益；不可合并叙事保留分支；Deferred 不影响本地纵切 |

`F1-DEV-04` 就是“统一人物编辑器规范”的正确派工方式：05 只提供角色正史、社会与表达边界；技术实现必须同时读取 06/11/12/13/14/16。若只写“参考 05 技术栈 2–3 部分”，团队会把世界观、Schema、编辑器、Runtime、资产和验收混成一份模糊需求。

### 4.4 任务卡复制模板

```text
任务 ID / 标题：
单一 Owner / 必审人：
玩家或生产价值：
阶段、优先级与前置：
权威输入：文档路径 + §章节；机器合同/Stable ID：
交付产物：代码、数据、资产、编辑器、迁移、文档：
非目标与禁止捷径：
验收：自动测试、人工路径、性能/兼容、证据包位置：
失败回退与风险触发器：
完成后回写：01/02/03/专业文档/Schema/ADR：
```

## 5. 工作流、依赖与唯一交付

```text
锁定工具链/CMake/架构守卫
              │
              ▼
GameCore Tick/命令/事件/回放 ──────► Axmol + Web Single 宿主
              │                              │
              ├────► 雨夜试伞 Gameplay ─────┤
              ├────► ContentCore/模板/包 ────┤
              ├────► IndexedDB/迁移 ────────┤
              └────► Sync IDL/冲突 Spike     │
                                             ▼
                                  浏览器纵切与 F1 证据包
```

| 流 | 最终 Owner（A） | 必审/协作 | 前置 | F1 最小交付 | 不允许的捷径 |
| --- | --- | --- | --- | --- | --- |
| Build/Core | 技术负责人 | C++ 核心、构建、QA | 锁定工具清单 | CMake targets、60 Hz Session、原生测试、回放 | IDE 私有工程、反向 include |
| Web Host | Web 负责人 | 表现、Core、UX、QA | Core facade | HTTPS Shell、WASM Single、Canvas/DOM、生命周期 | JS 重写玩法、只测 Chromium |
| Gameplay | 战斗负责人 | 系统、Core、关卡、动画、QA | Core 命令与碰撞合同 | 移动/跳跃、伞器、工灯、两敌人、Boss | 用动画帧直接判伤害 |
| Content Tool | 工具负责人 | 内容技术、作者、Runtime、QA | Contracts/Schema | NPC/Boss 创建、校验、烘焙、预览 | 手写最终包、编辑器另写规则 |
| Persistence | Runtime 负责人 | Web、后端、QA | Profile/ABI | IndexedDB v1、刷新/崩溃恢复、导出夹具 | `beforeunload` 作为唯一保存 |
| Sync Spike | 后端负责人 | Web、Core、安全、QA | Sync IDL/权威矩阵 | 幂等、双端分叉、冲突包；可用 mock OIDC | 让服务成为开机依赖 |
| QA/Release | QA 负责人 | 构建运维、各模块 Owner | 各流可测试产物 | 自动门、性能 trace、录屏、风险/缺陷清单 | 只交口头“我这里能跑” |

所有流共享同一纵切、稳定 ID、命令回放和构建 ID。独立技术 Spike 可以存在，但必须在合入前接回这条路径。

## 6. 0–30 / 31–60 / 61–90 天成果门

这是以“一支至少覆盖 C++ 核心、Web/客户端、Gameplay、工具/内容技术、QA，后端/运维可兼职接入的小型完整团队”为前提的成果窗口，不是无条件上线承诺。若实际容量不足，保持门的顺序并调整日期，禁止同时砍证据和内容深度。

| 窗口 | 成果门 | 最终 A | 必须看到 | Go/No-Go 决策 |
| --- | --- | --- | --- | --- |
| 0–30 天：G1 宿主可行性 | 可重现构建和斜向全景控制样片 | 技术负责人 | 干净环境 Bootstrap；Web Single/Windows 核心；八向/纵深输入、深度排序/遮挡、音频、固定 Tick、隐藏/恢复、context restore；初始体积/内存 | 保留 Axmol、维护小型补丁，或启动替代 Presentation ADR |
| 31–60 天：G2 可玩闭环 | “雨夜试伞”从标题页到结算可玩 | 游戏总监 | 两战斗系统最小交叉、两敌人、四时伞祟、DOM HUD、失败重试、IndexedDB 刷新/离线恢复 | 继续 F1；若手感/生命周期失败则停内容扩张 |
| 61–90 天：G3 可生产性 | 同一内容可由工具创建并跨 Web/Windows 验证 | 制作人 | NPC/Boss/任务模板、`.tgdpack` v1 草案、迁移、回放、同步冲突 Spike、性能/辅助/夜间 CI | 进入 M1、延长 F1、替换宿主，或把非关键同步/Pthreads 移出主路径 |

每个门只有上表一名最终 A。技术负责人必须签署架构/性能风险，QA 负责人必须签署证据完整性，安全/存档/Blocker 红线保留正式否决权；这些是会签/否决，不构成第二个 A。任一否决未关闭时，最终 A 只能作 No-Go。例外必须有单一 Owner、到期日、玩家影响、回滚和下一门关闭条件。

## 7. F1 浏览器支持矩阵

| 能力 | 必选 | 增强/后续 |
| --- | --- | --- |
| 桌面 Chrome/Edge 当前稳定与上一稳定 | 完整游戏、单线程 WASM | Pthreads、PWA 安装 |
| 桌面 Firefox 当前稳定与上一稳定 | 完整游戏、单线程 WASM | Pthreads 经独立门 |
| Safari/macOS | 启动、存档、核心纵切 Spike | 通过后加入正式矩阵 |
| 移动浏览器 | 能力探测、友好提示 | 触控/内存门通过后 Beta |
| WebGL2 不可用 | 清楚阻断与帮助 | 不维护 Canvas2D 完整回退 |
| 离线 | 已缓存内容、本地档可玩 | 未下载地区不伪装可用 |

不以 User-Agent 猜能力；启动时实际检测 WebAssembly、WebGL2、IndexedDB、存储额度、SharedArrayBuffer 和输入设备。

## 8. 1.0 内容规模门禁

| 指标 | 最低承诺 | 当前蓝图 | 验证方式 |
| --- | ---: | ---: | --- |
| 战斗系统 | 2 | 2 | 目录校验 + 实机组合测试 |
| 武器体系 | 2 | 2 | 完整架势/工艺/成长验收 |
| 地区 | 3 | 3 | 地区完成度矩阵 |
| 大型子地区 | 15 | 18 | 目录校验 + 关卡审查 |
| Boss | 10 | 14 | 多阶段游玩与回放 |
| 核心 NPC | 20 | 24 | 关系弧与同步回归 |
| 地区主线时长 | 300 分钟 | 各 315 分钟 | 首玩盲测中位数 |
| 总主线 | 900 分钟 | 945 分钟 | 三地区首玩盲测 |
| 主线章 | 18 | 21 | 任务图校验 |
| 成链支线 | 15 | 18 | 状态机与跨端恢复 |
| 兴趣点 | 100 | 120 | 地图密度与盲测 |

时长不能由加载、强制慢走、重复刷怪或无意义往返填充。浏览器资源分包也不能把下载等待算作游玩时间。

## 9. 功能完成定义

每项功能需提供八类证据：

1. **玩家价值**：解决什么体验问题。
2. **C++ 契约**：API、单位、所有权、稳定 ID 和错误语义。
3. **跨平台**：Web 单线程、Native 纯核心；相关时再含 Pthreads/移动端。
4. **生命周期**：启动、页面隐藏/恢复、刷新、离线、取消和退出。
5. **存档/同步**：本地事务、迁移、幂等、离线积压与冲突。
6. **内容证据**：教程、正式用例和变式/压力用例。
7. **测试/性能**：自动化、人工游玩、下载、内存、帧和弱网。
8. **维护**：文档、编辑器、日志、缓存失效与回滚。

缺任一适用证据只能标记“实现中”或“验证中”。

## 10. 内容完成定义

角色、Boss、子地区和任务仍遵循上一版深度门禁，并增加 Web/同步要求：

- 核心 NPC 的日程、关系和任务在刷新、离线积压和另一设备恢复后不死锁。
- Boss 的失败重试快照可写 IndexedDB，页面异常关闭后回到合法安全点。
- 子地区资源包可独立缓存、校验、清理和重新下载，不破坏持久状态。
- 任务奖励使用幂等 operation ID，重复同步不重复发放。
- 叙事不可自动合并冲突必须保留两份证据并让玩家明确选择。

## 11. Web 性能硬门

F1 起始目标，最终由真实设备校准：

- 推荐桌面 1080p 60 FPS；低档可锁 30 FPS，但规则仍为 60 Hz。
- 单线程 Web 构建 P95 主帧不超过 16.7 ms；页面后台不继续追赶无限 Tick。
- 冷启动首个可交互画面在 20 Mbps/常规延迟下目标 ≤ 8 秒，回访缓存目标 ≤ 3 秒。
- Brotli 后 WASM 目标 ≤ 12 MiB；首段必需资源总传输目标 ≤ 35 MiB。
- 活跃内存目标 ≤ 512 MiB，硬门 ≤ 768 MiB；长时游玩回到枢纽后应明显回落。
- 主线程不出现 >100 ms 的资源解析/解压峰值；内容包异步下载、分段解析和预算提交。

不得用“原生很快”推断 WASM、浏览器缓存或移动设备表现。

## 12. 风险与触发器

概率/影响在每次双周复审时更新；这里记录决策触发器，不伪造精确百分比。

| ID | 风险 | 概率/影响 | Owner | 触发器 | 缓解与失败回退 |
| --- | --- | --- | --- | --- | --- |
| R-01 | Axmol Web Preview 维护/兼容不足 | 中/极高 | 技术负责人 | G1 任一必选浏览器无法稳定启动/恢复，或需大面积侵入 Gameplay | 限时修宿主；无解则 ADR 比较 SDL/WebGL2 等替代 Presentation |
| R-02 | WASM 下载、线性内存、纹理上传超门 | 高/高 | Web 负责人 | 首访 >35 MiB、活跃内存 >768 MiB、重复进出不回稳 | 会同技术美术缩依赖/包/图集，切片解压上传；不靠删玩法时长掩盖 |
| R-03 | Pthreads 隔离破坏 CDN/OIDC/嵌入 | 中/中 | Web 负责人 | COOP/COEP 影响身份或第三方资源，或收益 <15% | 会同运维保持 Stable Single；Threads 使用独立渠道/origin 或延后 |
| R-04 | 工具落后导致手写内容特判 | 高/极高 | 工具负责人 | 第二个同类内容仍需改 Runtime/手写加载或复制脚本 | 停止扩内容，先补模板/校验/预览；M2 要求模板创建率 ≥90% |
| R-05 | 浏览器本地档被清理/事务失败 | 中/极高 | Runtime 负责人 | 配额、双标签、崩溃测试出现丢档/重复奖励 | 会同 Web 建立多 Head/备份/导出；云可选；未确认写入不显示已保存 |
| R-06 | 同步模型静默丢叙事/库存 | 中/极高 | 后端负责人 | 双端分叉无法解释或 Operation 重放非幂等 | 缩小自动合并域；保留两分支/复制 Profile；服务不阻塞单机 |
| R-07 | 多端并进稀释主线 | 高/高 | 制作人 | Web F1 未过就投入移动商店/最终适配 | Windows 仅对照；移动端延至独立触控/性能门 |
| R-08 | 内容规模超过团队产能 | 高/极高 | 制作人 | 江南金标准单元成本使 1.0 排期不可接受 | 调整团队、预算、内部/Preview 制作顺序；改变 1.0 三地区边界必须走 ADR 并同步 00/02/07，不能静默拆散或用重复刷怪填足 5 小时 |

F1 失败时先收缩宿主或替换 Presentation/Platform，而不是牺牲内容深度来迁就错误架构。

## 13. 标准证据包与放行

每次 G1/G2/G3 和后续里程碑必须生成同一结构的只读证据包：

- commit、build ID、渠道、工具链/依赖锁和产物哈希。
- 构建日志、自动测试报告、Schema/内容报告和已知 Flaky。
- 浏览器/设备/DPR/输入/网络矩阵；原始性能 trace 与内存曲线。
- 从标题页开始的无剪辑核心路径录屏，以及失败/刷新/离线/冲突录屏。
- 黄金回放、存档/迁移/同步夹具和内容包样本。
- Blocker/Critical/Major 清单、风险变化、例外及到期日。
- 上表对应里程碑最终 A 的 Go/No-Go 结论，以及技术、QA、安全/存档红线的会签或否决记录。

证据以 build ID 放在 CI artifact/受控归档，候选发布证据至少保留到该大版本停止支持；发布产物、迁移、黄金档和安全/许可记录按 17 的灾备策略长期留存。03 只写结果与证据位置，不复制整份报告。

## 14. 计划变更规则

- 改变 C++、Web 首发、Axmol/Emscripten、DOM UI、本地存档或同步模型必须写新 ADR。
- 平台可延后，但不能在未评估时宣称“多端已经支持”。
- 内容数量下调必须同步 00、02、07 和目录 JSON。
- 完成事实移入 03；02 只保留当前状态、风险和下一门禁。
